# Contributing

## Process

1. Create a feature branch from `main`
2. Make changes with clear, atomic commits
3. Open a Pull Request using the PR template
4. Wait for QA review approval
5. Merge after all checks pass

## Requirements

- All PRs require at least 1 review approval
- Branch protection enforced on `main`
- Web commit sign-off required

